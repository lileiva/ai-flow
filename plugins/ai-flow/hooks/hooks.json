{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "model": "sonnet",
            "prompt": "Check if `.ai-flow.json` exists with `linearSync: true` and if any flow phase completed during this session. If linear sync is enabled and a phase completed, block with the reason: Reminder: verify Linear issues are up to date via /flow-linear-sync. Otherwise, approve."
          }
        ]
      }
    ]
  }
}
